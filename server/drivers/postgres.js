export function createPostgresDatabase(config) {
  throw new Error(
    'PostgreSQL adapter is not wired yet. Install a PostgreSQL driver and implement createPostgresDatabase(config) in this file.'
  );
}
